{
  "name": "yuhan_yang_project3",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "concurrently": "^9.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.8.4"
  },
  "scripts": {
    "devstart": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "start": "npm run start --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "@babel/plugin-proposal-private-property-in-object": "^7.21.11"
  }
}
